<UserControl x:Class="ICSheet5e.Views.HealthChangeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ICSheet5e.Views"
             xmlns:tk="http://schemas.xceed.com/wpf/xaml/toolkit"
             mc:Ignorable="d" 
             d:DesignHeight="120" d:DesignWidth="300">
    <Border BorderBrush="Black" BorderThickness="4" Background="LightGray">
        <DockPanel>
            <Label DockPanel.Dock="Top" Margin="0,0,0,10">Amount</Label>
            <tk:IntegerUpDown DockPanel.Dock="Top" Margin="0,0,0,10" Value="{Binding Path=HealthChangeAmount}"/>
            <Button DockPanel.Dock="Bottom" Command="{Binding Path=DoHealthChangeCommand}">OK</Button>
        </DockPanel>
    </Border>
</UserControl>
