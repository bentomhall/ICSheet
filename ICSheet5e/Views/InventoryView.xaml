<UserControl x:Class="ICSheet5e.Views.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:ICSheet5e.ViewModels"
             xmlns:res="clr-namespace:ICSheet5e.Properties"
             xmlns:v="clr-namespace:ICSheet5e.Views"
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="900">
    <UserControl.Resources>
        <ImageBrush x:Key="HumanImageSource" ImageSource="../Resources/tall-human-silhouette.png"/>
        <v:ItemNameConverter x:Key="ItemNameConverter"/>
 
    </UserControl.Resources>
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="5*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="119*"/>
            <ColumnDefinition Width="181*"/>
        </Grid.ColumnDefinitions>
        <ListView x:Name="InventoryListView" Grid.RowSpan="1" ItemsSource="{Binding Path=Items}">
            <ListView.Resources>
                <ContextMenu x:Key="EquipmentContextMenu">
                    <MenuItem Header="Toggle Equipped" Command="{Binding Path=DataContext.EquipItemCommand, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ListView}}" CommandParameter="{Binding Path=SelectedItem, RelativeSource={RelativeSource AncestorType={x:Type ListView}}}"/>
                </ContextMenu>
            </ListView.Resources>
            <ListView.ItemContainerStyle>
                <Style TargetType="{x:Type ListViewItem}">
                    <Setter Property="ContextMenu" Value="{StaticResource EquipmentContextMenu}"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.View>
                <GridView>
                    <GridView.Columns>
                        <GridViewColumn Header="Name" Width="210" DisplayMemberBinding="{Binding Name}">
                        
                        </GridViewColumn>
                        <GridViewColumn Header="Count" Width="80" DisplayMemberBinding="{Binding Path=Count}"></GridViewColumn>
                        <GridViewColumn Header="Equipped">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding Path=IsEquipped, Mode=TwoWay}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView.Columns>
                </GridView>
            </ListView.View>
        </ListView>
        <Canvas Grid.Column="1" >
            <Canvas.Background>
                <ImageBrush ImageSource="../Resources/tall-human-silhouette.png"/>
            </Canvas.Background>
            <Label Width="100" Canvas.Left="222" Canvas.Top="10" Content="{Binding Path=EquippedItems[0]}" Foreground="{DynamicResource {x:Static SystemColors.ControlLightBrushKey}}" ></Label>
            <ComboBox Width="100" Canvas.Left="222" Canvas.Top="106"></ComboBox>
            <ComboBox Width="100" Canvas.Left="74" Canvas.Top="340"></ComboBox>
            <ComboBox Width="100" Canvas.Left="375" Canvas.Top="340"></ComboBox>
            <ComboBox Width="100" Canvas.Left="222" Canvas.Top="187"></ComboBox>
            <ComboBox Width="100" Canvas.Left="222" Canvas.Top="340"></ComboBox>
            <ComboBox Width="100" Canvas.Left="222" Canvas.Top="517"></ComboBox>
        </Canvas>
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="80*"/>
                <RowDefinition Height="80*"/>
                <RowDefinition Height="80*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="119*"/>
                <ColumnDefinition Width="119*"/>
                <ColumnDefinition Width="119*"/>
            </Grid.ColumnDefinitions>
            <Button>Increase Quantity</Button>
            <Button Grid.Column="1">Decrease Quantity</Button>
            <Button Grid.Column="2">Drop</Button>
            <Label Grid.Row="1" HorizontalContentAlignment="Center" VerticalContentAlignment="Center">Gold</Label>
            <Label Grid.Row="1" Grid.Column="1" HorizontalContentAlignment="Center" VerticalContentAlignment="Center">100</Label>
            <StackPanel Grid.Row="1" Grid.Column="2">
                <Button Height="40" Background="Red">Pay</Button>
                <Button Height="40" Background="Green">Gain</Button>
            </StackPanel>

        </Grid>
        <Grid Grid.Column="1" Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="44*"/>
                <RowDefinition Height="52*"/>
                <RowDefinition Height="45*"/>
                <RowDefinition Height="47*"/>
                <RowDefinition Height="52*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="131*"/>
                <ColumnDefinition Width="412*"/>
            </Grid.ColumnDefinitions>
            <Label HorizontalContentAlignment="Center" VerticalContentAlignment="Center">Item Name</Label>
            <TextBox Grid.Column="1" Background="BlanchedAlmond"></TextBox>
            <Label Grid.Row="1" VerticalContentAlignment="Center" HorizontalContentAlignment="Center">Item Properties</Label>
            <Grid Grid.Column="1" Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="83*"/>
                    <ColumnDefinition Width="82*"/>
                    <ColumnDefinition Width="82*"/>

                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="26*"/>
                    <RowDefinition Height="26*"/>
                </Grid.RowDefinitions>
                <Label>Weight (lbs)</Label>
                <TextBox Grid.Row="1"></TextBox>
                <Label Grid.Column="1">Value (gp)</Label>
                <TextBox Grid.Column="1" Grid.Row="1"></TextBox>
                <Label Grid.Column="2">Slot</Label>
                <ComboBox Grid.Column="2" Grid.Row="1"></ComboBox>
            </Grid>
            <Label Grid.Row="2" VerticalContentAlignment="Center" HorizontalContentAlignment="Center">Base Item</Label>
            <StackPanel Orientation="Horizontal" Grid.Column="1" Grid.Row="2">
                <ComboBox Width="235"></ComboBox>
                <Label Margin="10,0,0,0" VerticalContentAlignment="Center">Enhancement</Label>
                <TextBox Width="80"></TextBox>
            </StackPanel>
            <Label Grid.Row="3" HorizontalContentAlignment="Center" VerticalContentAlignment="Center">Properties:</Label>
            <TextBox Grid.Row="3" Grid.Column="1" Grid.RowSpan="2"></TextBox>
            <StackPanel Orientation="Horizontal" Grid.Row="4">
                <Button Width="66" Command="{Binding AddItemCommand}">Add</Button>
                <Button Width="66">Clear</Button>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
