<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ViewModels="clr-namespace:ICSheet5e.ViewModels" x:Class="ICSheet5e.MainWindow"
        Title="MainWindow" Height="350" Width="525" DataContext="{StaticResource ApplicationModel}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="PageStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <DockPanel >
        <Menu DockPanel.Dock="Top" Height="20">
            <MenuItem Header="_File" Margin="0" Width="56">
                <MenuItem Command="{Binding Path=NewCharacterCommand}" Header="_New"/>
                <MenuItem Command="{Binding Path=OpenCharacterCommand}" Header="_Open"/>
                <MenuItem Command="{Binding Path=SaveCharacterCommand}" Header="_Save"/>
            </MenuItem>
            <MenuItem Header="_Edit" IsCheckable="True"/>
        </Menu>
        <TabControl VerticalAlignment="Stretch" TabStripPlacement="Left" 
                    Style="{StaticResource SheetTabControlItem}"
                    ItemsSource="{Binding Tabs}">
            
        </TabControl>
    </DockPanel>

</Window>
